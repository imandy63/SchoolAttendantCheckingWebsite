/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ['fit.huit.edu.vn'], // Thêm domain ảnh bạn cần sử dụng
    },
  };
  
  module.exports = nextConfig;
  