export enum Role {
  ADMIN = "admin",
  STUDENT = "student",
  UNION_WORKER = "union_worker",
}

export enum Participation_Status {
  REGISTERED = "registered",
  REJECTED = "rejected",
  PARTICIPATED = "participated",
}
